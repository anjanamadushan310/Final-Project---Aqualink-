spring.application.name=aqualink
server.port=8080

# dataBase
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/aqualinkfinel28?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&sessionVariables=foreign_key_checks=0
spring.datasource.username=root
spring.datasource.password=ASD236asd

# Use create for development to recreate tables each time
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

#jpa vendor  adapter configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# NOTE: Removed conflicting hbm2ddl.auto property - using spring.jpa.hibernate.ddl-auto instead
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

#after jwt request filter clz
spring.main.allow-circular-references= true
#debug
logging.level.org.springframework.security=DEBUG
logging.level.com.example.aqualink=DEBUG

# File upload properties
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# File storage path
file.upload-dir=./uploads/
industrialimages.upload.dir=uploads/industrial_images
fishimages.upload.dir=./uploads/fish_images
app.file.storage.mapping=file:uploads/profile-images/





# Email configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shehan8998@gmail.com
spring.mail.password=hawq zncm dzse ucvj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# CORS Configuration
# For development - local origins
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
# For production, set environment variable:
# export CORS_ALLOWED_ORIGINS="https://your-frontend-domain.com,https://www.your-frontend-domain.com"

# JWT Security Configuration
# IMPORTANT: Change jwt.secret in production! Generate with: openssl rand -base64 32
jwt.secret=${JWT_SECRET:O6z6I2xL8UeQ9nV0xD5hRpO3rYgCmJv6YzNcT0qLgBw=}
jwt.expiration=28800000

# Note: For production, set JWT_SECRET environment variable:
# Linux/Mac: export JWT_SECRET="your-secure-secret-key"
# Windows: set JWT_SECRET=your-secure-secret-key

